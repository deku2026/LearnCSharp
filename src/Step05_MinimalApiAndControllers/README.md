# Step05 · Minimal API ↔ Controller

对应：`步骤5-MinimalAPI与Controller-完整实施指南.md`

主写 Minimal：`/api/products` CRUD + endpoint filter + SSE  
了解 Controller：`/api/students`

```bash
dotnet run --project src/Step05_MinimalApiAndControllers
# launchSettings → :5105
```
