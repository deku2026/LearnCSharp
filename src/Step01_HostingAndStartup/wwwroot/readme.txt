This file lives under the Web root (wwwroot).
Content root is the project/app base path; Web root defaults to {ContentRoot}/wwwroot.
Only files under Web root are served by static-file middleware by default.
